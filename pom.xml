<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.4.1</version>
        <relativePath />
    </parent>
    <groupId>com.secretsanta</groupId>
    <artifactId>api</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>Secret Santa API</name>
    <description>Secret Santa API</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-security</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-thymeleaf</artifactId>
        </dependency>
        
        <!-- hot swapping, disable cache for template, enable live reload -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-devtools</artifactId>
            <optional>true</optional>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
            <exclusions>
                <exclusion>
                    <groupId>org.mockito</groupId>
                    <artifactId>mockito-core</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        
        <!-- Mockito Inline for JDK 21+ compatibility -->
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-inline</artifactId>
            <version>5.2.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
        </dependency>

        <dependency>
            <groupId>org.liquibase</groupId>
            <artifactId>liquibase-core</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-mail</artifactId>
        </dependency>

        <dependency>
            <groupId>org.apache.commons</groupId>
            <artifactId>commons-lang3</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
        
        <!-- JWT Support -->
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-api</artifactId>
            <version>0.12.6</version>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-impl</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>io.jsonwebtoken</groupId>
            <artifactId>jjwt-jackson</artifactId>
            <version>0.12.6</version>
            <scope>runtime</scope>
        </dependency>
        
        <!-- SpringDoc OpenAPI 3.0 for API Documentation -->
        <!-- Provides OpenAPI 3.0 specification support and Swagger UI -->
        <!-- Access at: http://localhost:8080/swagger-ui/index.html -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>2.7.0</version>
        </dependency>

    </dependencies>

    <profiles>
        <!-- Profile for building with frontend (default) -->
        <profile>
            <id>with-frontend</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <build>
                <plugins>
                    <!-- Frontend Maven Plugin for building Angular app -->
                    <plugin>
                        <groupId>com.github.eirslett</groupId>
                        <artifactId>frontend-maven-plugin</artifactId>
                        <version>1.15.0</version>
                        <configuration>
                            <workingDirectory>frontend</workingDirectory>
                            <installDirectory>target</installDirectory>
                        </configuration>
                        <executions>
                            <!-- Install Node and npm -->
                            <execution>
                                <id>install node and npm</id>
                                <goals>
                                    <goal>install-node-and-npm</goal>
                                </goals>
                                <configuration>
                                    <nodeVersion>v22.13.0</nodeVersion>
                                    <npmVersion>10.9.2</npmVersion>
                                </configuration>
                            </execution>
                            <!-- Install npm dependencies -->
                            <execution>
                                <id>npm install</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <configuration>
                                    <arguments>install</arguments>
                                </configuration>
                            </execution>
                            <!-- Build Angular app -->
                            <execution>
                                <id>npm run build</id>
                                <goals>
                                    <goal>npm</goal>
                                </goals>
                                <configuration>
                                    <arguments>run build:prod</arguments>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        
        <!-- Profile for building Java only (no frontend) -->
        <profile>
            <id>java-only</id>
        </profile>
    </profiles>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            
            <!-- Maven Surefire Plugin for JDK 21+ compatibility -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>
                        -XX:+EnableDynamicAgentLoading
                    </argLine>
                </configuration>
            </plugin>
            
            <!-- Lines of Code Counter -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>3.1.0</version>
                <executions>
                    <execution>
                        <id>count-lines</id>
                        <phase>validate</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <echo message="=========================================="/>
                                <echo message="       Lines of Code Statistics"/>
                                <echo message="=========================================="/>
                                <exec executable="cmd" osfamily="windows" outputproperty="loc.java">
                                    <arg value="/c"/>
                                    <arg value="powershell"/>
                                    <arg value="-Command"/>
                                    <arg value="(Get-ChildItem -Path src -Include *.java -Recurse | Get-Content | Measure-Object -Line).Lines"/>
                                </exec>
                                <exec executable="sh" osfamily="unix" outputproperty="loc.java">
                                    <arg value="-c"/>
                                    <arg value="find src -name '*.java' -exec wc -l {} + | tail -1 | awk '{print $1}'"/>
                                </exec>
                                <exec executable="cmd" osfamily="windows" outputproperty="loc.ts">
                                    <arg value="/c"/>
                                    <arg value="powershell"/>
                                    <arg value="-Command"/>
                                    <arg value="(Get-ChildItem -Path frontend/src -Include *.ts -Recurse -ErrorAction SilentlyContinue | Get-Content | Measure-Object -Line).Lines"/>
                                </exec>
                                <exec executable="sh" osfamily="unix" outputproperty="loc.ts">
                                    <arg value="-c"/>
                                    <arg value="find frontend/src -name '*.ts' -exec wc -l {} + 2>/dev/null | tail -1 | awk '{print $1}' || echo 0"/>
                                </exec>
                                <exec executable="cmd" osfamily="windows" outputproperty="loc.html">
                                    <arg value="/c"/>
                                    <arg value="powershell"/>
                                    <arg value="-Command"/>
                                    <arg value="(Get-ChildItem -Path frontend/src -Include *.html -Recurse -ErrorAction SilentlyContinue | Get-Content | Measure-Object -Line).Lines"/>
                                </exec>
                                <exec executable="sh" osfamily="unix" outputproperty="loc.html">
                                    <arg value="-c"/>
                                    <arg value="find frontend/src -name '*.html' -exec wc -l {} + 2>/dev/null | tail -1 | awk '{print $1}' || echo 0"/>
                                </exec>
                                <echo message="Java files:        ${loc.java} lines"/>
                                <echo message="TypeScript files:  ${loc.ts} lines"/>
                                <echo message="HTML files:        ${loc.html} lines"/>
                                <echo message="=========================================="/>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

</project>
